export type DaoEdge = {
    id: string;
    from: string;
    to: string;
    texts: string[];
    value: number;
  };
  
  export type DaoNode = {
    id: string;
    label: string;
    title: string;
    value: number;
  }
  